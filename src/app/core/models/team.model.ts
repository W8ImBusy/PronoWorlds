export class Team {
    name: string;
    region: string;
    seed: number;

    constructor(name: string, region: string, seed: number){
        this.name = name;
        this.region = region;
        this.seed = seed;
        
    }
}